<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.liyy.mytest">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_OVERLAY_WINDOW" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.PlayMusicActivity"></activity>
        <activity android:name=".activity.HttpPostActivity" />
        <activity android:name=".activity.HttpGetActivity" />
        <activity android:name=".activity.LoadPageActivity" />
        <activity android:name=".activity.SetResolutionActivity" />
        <activity
            android:name=".activity.MainActivity"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.LoadPageX5Activity"
            android:configChanges="keyboardHidden|orientation|screenSize" />
        <activity android:name=".activity.AlertDialogActivity" />
        <activity android:name=".activity.CalculatorActivity" />
        <activity android:name=".activity.BaseActivity" />
    </application>

</manifest>